# Stockflow frontend

Phase 1 provides the Vite/React project, authentication, protected application layout, sidebar/topbar, notification foundation, and dashboard wired to `GET /api/reports/dashboard`.

1. Copy `.env.example` to `.env` and set `VITE_API_URL`.
2. Run `npm install`.
3. Run `npm run dev`.
