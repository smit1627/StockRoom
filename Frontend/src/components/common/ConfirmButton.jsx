import {useState} from "react";export default function ConfirmButton({onConfirm,children,className="btn btn-outline-danger btn-sm"}){const [ask,setAsk]=useState(false);return ask?<span className="confirm-inline"><button className="btn btn-danger btn-sm" onClick={async()=>{await onConfirm();setAsk(false)}}>Confirm</button><button className="btn btn-light btn-sm" onClick={()=>setAsk(false)}>Cancel</button></span>:<button className={className} onClick={()=>setAsk(true)}>{children}</button>}
