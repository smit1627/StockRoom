export function Skeleton({rows=1}){return <>{Array.from({length:rows},(_,i)=><div className="skeleton" key={i}/>)}</>}export function Empty({icon="inbox",title="Nothing here yet",text="There is no data to display."}){return <div className="empty-state"><i className={`bi bi-${icon}`}/><h3>{title}</h3><p>{text}</p></div>}export function ErrorState({onRetry}){return <div className="empty-state"><i className="bi bi-exclamation-circle text-danger"/><h3>Unable to load data</h3><p>Please check your connection and try again.</p><button className="btn btn-outline-primary" onClick={onRetry}>Try again</button></div>}
