import { useContext } from "react";
import { useLocation } from "react-router-dom";
import { AppContext } from "../../context/AppContext";
const names = { "/dashboard": ["Dashboard", "Your inventory command centre"], "/products": ["Parts catalogue", "Manage loom components and stock levels"], "/transactions": ["Stock movements", "Track every receipt, issue and adjustment"], "/categories": ["Part families", "Organise your catalogue for faster work"], "/reports": ["Reports", "Operational insight, ready to export"], "/settings": ["Company settings", "Workspace identity and branding"] };
export default function Topbar({ onMenu }) { const location = useLocation(); const { company } = useContext(AppContext); const [title, subtitle] = names[location.pathname] || ["Veer Inventory", "Textile spare-parts operations"]; return <header className="topbar"><button className="menu-button" onClick={onMenu} aria-label="Open navigation"><i className="bi bi-list"/></button><div><p className="topbar-kicker">{company?.name || "VEER ENTERPRISE"}</p><h1>{title}</h1><p className="topbar-subtitle">{subtitle}</p></div><div className="top-actions"><span className="workspace-status"><i className="bi bi-circle-fill"/> Live workspace</span></div></header>; }
