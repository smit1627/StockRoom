import {useEffect,useState} from "react";import {Link} from "react-router-dom";import {listTransactions} from "../../api/transactionApi";import {formatCurrency} from "../../utils/formatCurrency";import {formatDate} from "../../utils/formatDate";import {Skeleton,Empty,ErrorState} from "../../components/common/PageState";export default function TransactionsPage(){const [data,setData]=useState(),[type,setType]=useState(''),[error,setError]=useState(false);const load=()=>{setError(false);listTransactions({type,limit:20}).then(r=>setData(r.data.data)).catch(()=>setError(true))};useEffect(load,[type]);if(error)return <ErrorState onRetry={load}/>;return <><div className="page-heading"><div><p className="eyebrow">INVENTORY ACTIVITY</p><h2>Transactions</h2></div><Link className="btn btn-primary" to="/transactions/new"><i className="bi bi-plus-lg"/> New transaction</Link></div><section className="card-panel"><div className="filter-row"><select className="form-select" value={type} onChange={e=>setType(e.target.value)}><option value="">All transaction types</option>{['opening_stock','stock_in','stock_out','adjustment'].map(t=><option key={t}>{t}</option>)}</select></div>{!data?<Skeleton rows={9}/>:data.transactions?.length?<div className="table-responsive"><table className="table clean-table"><thead><tr><th>Reference</th><th>Date</th><th>Type</th><th>Items</th><th>Total quantity</th><th>Total value</th><th>Remarks</th></tr></thead><tbody>{data.transactions.map(t=><tr key={t.id}><td><Link to={`/transactions/${t.id}`}>{t.transactionNumber}</Link></td><td>{formatDate(t.transactionDate)}</td><td><span className={`type-badge ${t.type}`}>{t.type.replace('_',' ')}</span></td><td>{t.items?.length||0}</td><td>{t.totalQuantity}</td><td>{formatCurrency(t.totalValue)}</td><td>{t.remarks||'—'}</td></tr>)}</tbody></table></div>:<Empty icon="receipt" title="No transactions found"/>}</section></>}
