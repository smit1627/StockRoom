import api from "./axios";export const listProducts=params=>api.get("/products",{params});export const getProduct=id=>api.get(`/products/${id}`);export const saveProduct=(id,body)=>api({method:id?"put":"post",url:id?`/products/${id}`:"/products",data:body,headers:{"Content-Type":"multipart/form-data"}});export const removeProduct=id=>api.delete(`/products/${id}`);export const productTransactions=id=>api.get(`/products/${id}/transactions`);
