import api from "./axios";export const listCategories=()=>api.get("/categories");export const saveCategory=(id,body)=>api({method:id?"put":"post",url:id?`/categories/${id}`:"/categories",data:body});export const removeCategory=id=>api.delete(`/categories/${id}`);export const addSubcategory=(id,name)=>api.post(`/categories/${id}/subcategories`,{name});export const removeSubcategory=(id,subId)=>api.delete(`/categories/${id}/subcategories/${subId}`);
