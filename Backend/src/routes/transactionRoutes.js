const router=require("express").Router(),c=require("../controllers/transactionController"),{requireAuth,allowRoles}=require("../middleware/auth");router.use(requireAuth);router.get("/product/:productId",c.byProduct);router.get("/:id",c.get);router.get("/",c.list);router.post("/",allowRoles("admin","staff"),c.create);router.delete("/:id",allowRoles("admin"),c.remove);module.exports=router;
