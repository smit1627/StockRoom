const router=require("express").Router(),c=require("../controllers/productController"),{requireAuth,allowRoles}=require("../middleware/auth"),{uploadImage,uploadError}=require("../middleware/upload");router.use(requireAuth);router.get("/",c.list);router.get("/:id/transactions",c.transactions);router.get("/:id/stock-history",c.transactions);router.get("/:id",c.get);router.post("/",allowRoles("admin"),uploadImage,uploadError,c.create);router.put("/:id",allowRoles("admin"),uploadImage,uploadError,c.update);router.delete("/:id",allowRoles("admin"),c.remove);module.exports=router;
