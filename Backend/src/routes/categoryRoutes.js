const router=require("express").Router(),c=require("../controllers/categoryController"),{requireAuth,allowRoles}=require("../middleware/auth");router.use(requireAuth);router.get("/",c.list);router.get("/:id",c.get);router.post("/",allowRoles("admin"),c.create);router.put("/:id",allowRoles("admin"),c.update);router.delete("/:id",allowRoles("admin"),c.remove);router.post("/:id/subcategories",allowRoles("admin"),c.addSubcategory);router.delete("/:id/subcategories/:subcategoryId",allowRoles("admin"),c.removeSubcategory);module.exports=router;
